# ── MEGqc dev requirements ─────────────────────────────────────────
# All dependency versions are defined in pyproject.toml (single source
# of truth).  This file just points there so `pip install -r
# requirements.txt` still works for convenience.
#
# Install variants:
#   pip install .            → core only (headless / HPC)
#   pip install .[gui]       → core + desktop GUI
#   pip install .[accel]     → core + numba acceleration
#   pip install .[all]       → everything
#   pip install -e .[all]    → editable dev install (recommended)
# ───────────────────────────────────────────────────────────────────
-e .[all]
